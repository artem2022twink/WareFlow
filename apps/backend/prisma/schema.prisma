generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Product {
  id       Int    @id @default(autoincrement())
  name     String
  price    Int
  category String
  stock    Int
}
